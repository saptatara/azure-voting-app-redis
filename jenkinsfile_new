pipeline {
    agent any

    stages {
        stage('Verify_Branch') {
            steps {
                def GIT_BRANCH = sh(returnStdout: true, script: 'git rev-parse --abbrev-ref HEAD').trim()
                echo $GIT_BRANCH
            }
        }
        
        }
        
    }

